apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "seminar.fullname" . }}-test-connection"
  labels:
    {{- include "seminar.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "seminar.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
